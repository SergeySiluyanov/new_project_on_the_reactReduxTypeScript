import * as React from 'react';

class mainInfo extends React.Component {
    render() {
        return(
            <div>
                Hello, World!
            </div>
        );
    }
}

export default mainInfo;